// WhatsAppButton — filled pill in WhatsApp green (#25D366).
// Links to https://wa.me/[number]. On mobile, a persistent fixed bar
// variant lives at the bottom of the viewport (DESIGN.md §8 key decision 4).

export interface WhatsAppButtonProps {
  /** Render as the fixed mobile bar. Defaults to inline. */
  fixedMobile?: boolean;
}

export function WhatsAppButton(_: WhatsAppButtonProps) {
  return null;
}

export default WhatsAppButton;
